services:
  - type: web
    name: trustpilot-api
    rootDir: . # Changed to .
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: PYTHONPATH=. uvicorn main:app --host 0.0.0.0 --port $PORT # Changed startCommand
    autoDeploy: false  # déploiement déclenché par Deploy Hook après tests
    envVars:
      - key: ENV
        value: prod
      - key: RENDER
        value: "true"
