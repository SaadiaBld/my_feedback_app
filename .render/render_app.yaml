services:
  - type: web
    name: trustpilot-app
    env: python
    plan: free
    branch: render
    buildCommand: pip install -r app/requirements.txt
    startCommand: python app/run_app.py
    envVars:
      - key: ENV
        value: production
    autoDeploy: true
    rootDir: .  # racine du repo
